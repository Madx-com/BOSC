#include <stdio.h>
#include "redirect.h"

int main(int argc, char *argv[])
{
  bool b = false;
  while(!b)
  {

    char *p;

    scanf( %c , &p);
    printf("%s",&p);
    if('e' == *p)
    {
      b = true;
    }
  }
  return 0;
}
